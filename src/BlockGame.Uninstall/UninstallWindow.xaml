<Window x:Class="BlockGame.Uninstall.UninstallWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:BlockGame.Uninstall"
        Title="卸载 BlockGame"
        Width="500"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="卸载 BlockGame"
                       FontSize="24"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource TextBrush}" />
            <TextBlock x:Name="DescriptionText"
                       Margin="0,7,0,0"
                       Text="请输入管理密码。保护处于锁定状态时，必须先在管理界面完成解除流程。"
                       TextWrapping="Wrap"
                       LineHeight="22"
                       Foreground="{StaticResource MutedTextBrush}" />
        </StackPanel>

        <StackPanel x:Name="PasswordPanel" Grid.Row="1" Margin="0,22,0,0">
            <TextBlock Text="管理密码" Margin="0,0,0,6" />
            <local:TransientPasswordBox x:Name="PasswordInput"
                                        KeyDown="PasswordInput_KeyDown" />
        </StackPanel>

        <Grid Grid.Row="2" Margin="0,22,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center"
                       Text="卸载会删除本机规则、配置和审计日志。"
                       Foreground="#B42318" />
            <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Style="{StaticResource FlatButton}"
                        Content="取消"
                        Click="CancelButton_Click" />
                <Button x:Name="UninstallButton"
                        Style="{StaticResource UninstallButton}"
                        Margin="0"
                        Content="验证并卸载"
                        Click="UninstallButton_Click" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
